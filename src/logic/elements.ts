const elements: {
  button: HTMLButtonElement;
  playField: HTMLUListElement;
} = {
  button: document.getElementById("get_emoji") as HTMLButtonElement,
  playField: document.getElementById("play_field") as HTMLUListElement,
};

export default elements;
